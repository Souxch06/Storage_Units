<?xml version="1.0" encoding="UTF-8"?>
<!--
    ~ Storage Units - Plugin Minecraft (Paper / Purpur 1.21.11+)
    ~ Permet de stocker une grande quantité d'un seul type d'objet dans des coffres "Unités de stockage".
    ~ Compatible Java + Bedrock (Geyser/Floodgate).
    ~
    ~ Auteur : Souxch06
    ~ Licence : MIT
    -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>fr.souxch06</groupId>
    <artifactId>StorageUnits</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>StorageUnits</name>
    <description>Plugin Paper/Purpur ajoutant des coffres "Unités de stockage" qui contiennent
        un seul type d'item en grande quantité. Compatible Java et Bedrock (Geyser/Floodgate).</description>
    <url>https://github.com/Souxch06/Storage_Units</url>

    <organization>
        <name>Souxch06</name>
        <url>https://github.com/Souxch06</url>
    </organization>

    <!--
        Assistance au développement fournie par Arena Agent (MiniMax-M3),
        agent IA de la plateforme Arena.ai.
    -->

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>

        <!--
            Paper API 1.21.11.
            On utilise l'API "paper-api" qui contient déjà Bukkit + Spigot + Paper.
            Pour Paper 1.21.11, l'artefact devient : io.papermc.paper:paper-api
            -->
        <paper.api.version>1.21.1-R0.1-SNAPSHOT</paper.api.version>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
            <id>placeholderapi</id>
            <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
        </repository>
    </repositories>

    <dependencies>
        <!-- API Paper 1.21.11 (Bukkit + Spigot + Paper inclus) -->
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>${paper.api.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- SnakeYAML (utilisé par Bukkit pour la sérialisation) -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>2.2</version>
            <scope>provided</scope>
        </dependency>

        <!-- JUnit 5 pour les tests unitaires -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.name}-${project.version}</finalName>
        <defaultGoal>clean package</defaultGoal>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>plugin.yml</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>plugin.yml</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${maven.compiler.source}</source>
                    <target>${maven.compiler.target}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.3</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
